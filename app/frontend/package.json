{
  "name": "saiiut-frontend",
  "version": "1.0.0",
  "main": "main_electron.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . SAIIUT --platform=win32 --arch=x64 --out=dist --overwrite --ignore=dist --ignore=package-lock.json --ignore=README.md",
    "build": "electron-builder"
  },
  "dependencies": {
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "electron": "^36.4.0",
    "electron-packager": "^17.1.1"
  }
}
